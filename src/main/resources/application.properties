spring.application.name=figuredb

# ---------------------------------------------------------------------------
# Database: PostgreSQL
#
# Prima di avviare, creare il database vuoto:  createdb figuredb
#
# Le credenziali NON sono scritte qui: vengono lette da variabili d'ambiente,
# cosi' ognuno usa le proprie e nessuna password finisce su Git.
# La sintassi ${NOME:default} significa "usa la variabile NOME, e se non
# esiste usa il valore dopo i due punti".
#
# Per impostarle una volta sola su Windows, in PowerShell:
#     setx DB_USERNAME "postgres"
#     setx DB_PASSWORD "la-tua-password"
# poi CHIUDI e RIAPRI il terminale (setx vale dalle finestre nuove).
# ---------------------------------------------------------------------------
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/figuredb}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver

# ---------------------------------------------------------------------------
# JPA / Hibernate
#
# create-drop: a ogni avvio lo schema viene ricreato da zero e il
# DatabaseSeeder ricarica il catalogo dai CSV di src/main/resources/dati.
# Modifichi un CSV, riavvii, vedi il risultato: nessun passaggio da pgAdmin.
#
# E' la scelta giusta perche' nel database non c'e' nulla di insostituibile:
# tutti i dati vivono in file versionati con il progetto. Ha anche il
# vantaggio che aggiungere un attributo a un'entita' non richiede migrazioni.
#
# ATTENZIONE: allo spegnimento le tabelle vengono cancellate. Tutto cio' che
# inserisci dall'interfaccia del sito (recensioni, voci di collezione, figure
# create da amministratore) va perso al riavvio successivo. Se ti serve che
# sopravviva, mettilo nei CSV oppure passa temporaneamente a "update".
#
# In produzione si userebbe "validate".
# ---------------------------------------------------------------------------
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.open-in-view=false
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Batch e ordinamento delle scritture: riducono il numero di round-trip
spring.jpa.properties.hibernate.jdbc.batch_size=25
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true

# ---------------------------------------------------------------------------
# Thymeleaf
# ---------------------------------------------------------------------------
spring.thymeleaf.cache=false

# ---------------------------------------------------------------------------
# Messaggi di errore leggibili durante lo sviluppo
# ---------------------------------------------------------------------------
server.error.include-message=always
server.error.include-binding-errors=always
